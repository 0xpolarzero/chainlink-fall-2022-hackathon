{
  "name": "node-docker-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --inspect=0.0.0.0:9229 server.js",
    "test": "mocha ./**/*.js",
    "ci": "yarn install --immutable --immutable-cache --check-cache && yarn test"
  },
  "dependencies": {
    "nodemon": "^2.0.20",
    "ronin-database": "^0.1.1",
    "ronin-mocks": "^0.1.11",
    "ronin-server": "^0.1.3"
  },
  "devDependencies": {
    "mocha": "^10.1.0"
  }
}
